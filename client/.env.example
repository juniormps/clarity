# Client

# URL base da API. Em desenvolvimento, deixe vazio para usar caminhos
# relativos `/api` (o Vite faz o proxy para http://localhost:3000). Em
# produção, informe a URL completa da API, por exemplo:
# VITE_API_URL=https://api.exemplo.com
VITE_API_URL=
